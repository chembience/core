#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "ℹ️  test-build-all now runs the isolated full integration suite."
exec "$script_dir/test" --integration
