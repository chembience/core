services:
  django:
    image: ${CHEMBIENCE_PROD_IMAGE_NAME:?Run django-prepare-prod first}:${CHEMBIENCE_IMAGE_TAG:?Set CHEMBIENCE_IMAGE_TAG in .env.prod}
    env_file: .env.prod
    environment:
      CHEMBIENCE_RUNTIME_MODE: prod
    ports:
      - "${DJANGO_CONNECTION_PORT:-8001}:8000"
    restart: unless-stopped
    command: ["python", "-m", "gunicorn", "src.wsgi:application", "--bind", "0.0.0.0:8000", "--workers", "${GUNICORN_WORKERS:-8}"]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/healthz/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 45s

  migrate:
    image: ${CHEMBIENCE_PROD_IMAGE_NAME:?Run django-prepare-prod first}:${CHEMBIENCE_IMAGE_TAG:?Set CHEMBIENCE_IMAGE_TAG in .env.prod}
    env_file: .env.prod
    environment:
      CHEMBIENCE_RUNTIME_MODE: prod
    profiles: ["migration"]
    command: ["python", "manage.py", "migrate", "--noinput"]
