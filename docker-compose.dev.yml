version: "3.9"

services:
  jupyter:
    # Development convenience: disable token. Do NOT use in production.
    command: ["jupyter", "lab", "--ip=0.0.0.0", "--port=8888", "--no-browser", "--allow-root", "--ServerApp.token=''"]
