services:
  rdkit:
    image: ${CHEMBIENCE_PROD_IMAGE_NAME:?Run rdkit-prepare-prod first}:${CHEMBIENCE_IMAGE_TAG:?Set CHEMBIENCE_IMAGE_TAG in .env.prod}
    env_file: .env.prod
    environment:
      CHEMBIENCE_RUNTIME_MODE: prod
    restart: unless-stopped
    command: ["tail", "-f", "/dev/null"]
